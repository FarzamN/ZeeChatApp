import { StatusBar, StyleSheet } from 'react-native';

export const GlobalStyle = StyleSheet.create({
    LottieView: {
        height: 150,
        alignSelf: 'center',
    },

    Image: {
        width: '100%',
        height: '100%',
    },
})
